# jo-ppopgi

제비뽑기 방식으로 조를 나누는 웹앱 — 이름을 넣고 클릭하면 통에서 종이를 뽑아 조가 정해진다

커뮤니티(brag.io.kr) 공유용 사이드 앱. Next.js(App Router) + Vercel 배포.

## 개발
```bash
npm install
npm run dev      # http://localhost:3000
```

## 배포
side-app 작업공간에서: `bash scripts/deploy.sh jo-ppopgi`
